@page "/admin/user"
@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Admin - User";
}
@using Microsoft.AspNetCore.Mvc.RazorPages
@using Microsoft.EntityFrameworkCore
@using the_movie_hub.Models;
@model the_movie_hub.Pages.Admin.User.UserModel;

@* <div class="text-center max-w-1200 mx-auto py-8 px-21 rounded-lg text-white bg-slate-700">
<h1 value="">Users (@Model.Users.Count())</h1>
<a href="/Admin">Admin</a>

<div class="flex flex-col gap-4 text-left">
@foreach (var user in Model.Users)
{
<div class="flex gap-3">
<div class="flex gap-2">
<span>@user.FullName</span> -
<span>@user.Username</span> -
</div>

<form method="post">
<input type="hidden" name="Id" value="@user.Id" />
<button asp-page-handler="Delete" style="color: red">Delete</button>
</form>
</div>
}
</div>
</div> *@
<div class="p-21">
    <div class="p-21 bg-white text-dark rounded-lg shadow-lg overflow-x-scroll">
        <h2 class="text-2xl font-bold mb-6">Danh sách người dùng</h2>
        <table class="min-w-full border border-gray-300">
            <thead>
                <tr>
                    <th class="py-2 px-4 border border-gray-300">Id</th>
                    <th class="py-2 px-4 border border-gray-300">FullName</th>
                    <th class="py-2 px-4 border border-gray-300">Username</th>
                    <th class="py-2 px-4 border border-gray-300">Email</th>
                    <th class="py-2 px-4 border border-gray-300">Phone</th>
                    <th class="py-2 px-4 border border-gray-300">Password</th>
                    <th class="py-2 px-4 border border-gray-300">AuthType</th>
                    <th class="py-2 px-4 border border-gray-300">Birthday</th>
                    <th class="py-2 px-4 border border-gray-300">Expended</th>
                    <th class="py-2 px-4 border border-gray-300">CCCD</th>
                </tr>
            </thead>
            <tbody class="w-full">
                @foreach (var user in @Model.Users)
                {
                    <tr>
                        <td class="py-2 px-4 text-center border border-gray-300">@user.Id</td>
                        <td class="py-2 px-4 text-center border border-gray-300">@user.FullName</td>
                        <td class="py-2 px-4 text-center border border-gray-300">@user.Username</td>
                        <td class="py-2 px-4 text-center border border-gray-300">@user.Email</td>
                        <td class="py-2 px-4 text-center border border-gray-300">@user.Phone</td>
                        <td class="py-2 px-4 text-center border border-gray-300">@user.Password</td>
                        <td class="py-2 px-4 text-center border border-gray-300">@user.AuthType</td>
                        <td class="py-2 px-4 text-center border border-gray-300">@user.Birthday</td>
                        <td class="py-2 px-4 text-center border border-gray-300">@user.Expended</td>
                        <td class="py-2 px-4 text-center border border-gray-300">@user.CCCD</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

</div>
